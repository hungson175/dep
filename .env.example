# Where your llama.cpp server listens.
DEP_LLAMA_URL=http://127.0.0.1:8080

# Only if you started llama-server with --api-key / --api-key-file.
# DEP_API_KEY=
# DEP_API_KEY_FILE=~/.secrets/llama-key.txt

# Must match llama-server's -np.
DEP_SLOTS=8
